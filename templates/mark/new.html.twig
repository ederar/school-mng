{% extends 'admin/admin_base.html.twig' %}

{% block title %}New Mark{% endblock %}

{% block body %}
    <div class="container my-5">
     <h1 class="text-center mb-5">Create new Mark</h1>
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                 {{ include('mark/_form.html.twig') }}

                <a href="{{ path('mark_index') }}" >back to list</a>
            </div>
        </div>
    </div>
{% endblock %}
