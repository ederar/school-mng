{% extends 'admin/admin_base.html.twig' %}

{% block title %}Edit Mark{% endblock %}

{% block body %}
    <div class="container my-5">
         <h1 class="text-center mb-5">Edit Mark</h1>

    {{ include('mark/_form.html.twig', {'button_label': 'Update'}) }}

     {{ include('mark/_delete_form.html.twig') }}
     
    <a href="{{ path('mark_index') }}">back to list</a>
    </div>
{% endblock %}
